const userRepository = require("./user.repository");
const validationCodeRepository = require("./validationCode.repository");

module.exports = {
  userRepository: userRepository,
  validationCodeRepository: validationCodeRepository,
};
