const userRepository = require("./userRepository");
const validationCodeRepository = require("./validationCodeRepository");

module.exports = {
  userRepository: userRepository,
  validationCodeRepository: validationCodeRepository,
};
